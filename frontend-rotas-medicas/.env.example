# Deixe em branco (ou remova) para desenvolvimento local — as chamadas usam
# o proxy do Vite (vite.config.js) para http://localhost:8000.
# Em produção, aponte para a URL pública da API:
VITE_API_URL=https://api-rotas-medicas.onrender.com
